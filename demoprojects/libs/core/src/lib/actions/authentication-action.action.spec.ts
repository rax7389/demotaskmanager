import { AuthenticationAction } from './authentication-action.action';

describe('AuthenticationAction', () => {
  it('should create an instance', () => {
    expect(new AuthenticationAction()).toBeTruthy();
  });
});
