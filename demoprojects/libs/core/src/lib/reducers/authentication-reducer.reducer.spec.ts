import { AuthenticationReducer } from './authentication-reducer.reducer';

describe('AuthenticationReducer', () => {
  it('should create an instance', () => {
    expect(new AuthenticationReducer()).toBeTruthy();
  });
});
