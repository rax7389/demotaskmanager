import { ResponseHeaders } from './response-headers.middleware';

describe('ResponseHeaders', () => {
  it('should create an instance', () => {
    expect(new ResponseHeaders()).toBeTruthy();
  });
});
